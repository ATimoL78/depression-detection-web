# 快速开始指南

## 🚀 5分钟快速启动

### 1. 解压项目
```bash
tar -xzf depression-detection-web-optimized-final.tar.gz
cd depression-detection-web
```

### 2. 安装依赖
```bash
pnpm install
```

如果没有安装pnpm:
```bash
npm install -g pnpm
```

### 3. 配置环境变量

创建 `.env` 文件:
```bash
cat > .env << 'EOF'
# 数据库配置(可选,默认使用SQLite)
DATABASE_URL=mysql://root:password@localhost:3306/depression_detection

# OpenAI API Key(用于AI日记分析)
OPENAI_API_KEY=sk-your-api-key-here

# 会话密钥(随机生成)
SESSION_SECRET=your-random-secret-key-here
EOF
```

**注意**: 
- 如果没有OpenAI API Key,AI日记分析功能将返回默认建议
- 如果没有配置MySQL,系统将使用SQLite(开发模式)

### 4. 启动开发服务器
```bash
pnpm run dev
```

### 5. 访问系统
打开浏览器访问: http://localhost:3000

---

## 📱 核心功能使用

### 功能1: 高精度3D面部扫描

1. 点击"实时智能识别"
2. 点击"启动高精度3D面部扫描"
3. 允许摄像头权限
4. 保持面部在画面中央
5. 查看右侧AU分析和抑郁症风险评估

**特色**:
- ✅ 点云持久化显示(短暂遮挡不消失)
- ✅ 卡尔曼滤波平滑(无抖动)
- ✅ 8个AU精准分析
- ✅ 抑郁症风险评分

### 功能2: 标准化量表评估

1. 点击"标准化量表"
2. 选择PHQ-9或GAD-7
3. 根据过去两周情况如实填写
4. 查看评分和建议
5. 保存评估结果

**PHQ-9评分标准**:
- 0-4分: 无抑郁
- 5-9分: 轻度抑郁
- 10-14分: 中度抑郁
- 15-19分: 中重度抑郁
- 20-27分: 重度抑郁

### 功能3: 情绪日记 + AI分析

1. 点击"情绪日记"
2. 拖动滑块选择今天的心情(1-10分)
3. 选择今天做的活动
4. 填写CBT思维记录:
   - 发生了什么事?
   - 你的感受是什么?
   - 你做了什么?
5. 点击"AI思维模式分析"
6. 查看AI识别的负面思维模式和认知重构建议
7. 保存日记

**AI分析示例**:
```
识别到的负面思维模式:
• 灾难化思维: "这次失败意味着我永远不会成功"
• 黑白思维: "如果不是完美的,就是失败的"

认知重构建议:
• 尝试用"这次没成功,但我可以从中学习"替代
• 提醒自己:成功和失败之间有很多中间状态
```

### 功能4: 趋势分析

1. 点击"趋势分析"
2. 查看可视化图表:
   - 30天情绪趋势
   - PHQ-9评分历史
   - AU面部表情模式
   - 活动频率统计
3. 阅读AI洞察和个性化建议

---

## 🔧 常见问题

### Q1: 摄像头无法启动?
**A**: 检查浏览器权限设置,确保允许访问摄像头。推荐使用Chrome/Edge浏览器。

### Q2: AI分析失败?
**A**: 检查`.env`文件中的`OPENAI_API_KEY`是否正确配置。如果没有API Key,系统会返回默认建议。

### Q3: 数据库连接失败?
**A**: 
- 检查MySQL是否启动
- 检查`DATABASE_URL`配置是否正确
- 开发模式下可以不配置数据库,系统会使用SQLite

### Q4: 端口3000被占用?
**A**: 系统会自动寻找可用端口(3000-3020),查看终端输出的实际端口号。

### Q5: 构建失败?
**A**: 
```bash
# 清理缓存重新安装
rm -rf node_modules pnpm-lock.yaml
pnpm install
pnpm run build
```

---

## 📦 生产部署

### 1. 构建生产版本
```bash
pnpm run build
```

### 2. 启动生产服务器
```bash
NODE_ENV=production pnpm start
```

### 3. 使用PM2守护进程
```bash
npm install -g pm2
pm2 start dist/index.js --name depression-detection
pm2 save
pm2 startup
```

### 4. Nginx反向代理
```nginx
server {
  listen 80;
  server_name your-domain.com;
  
  location / {
    proxy_pass http://localhost:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
  }
}
```

---

## 📚 文档索引

- **README_OPTIMIZED.md** - 完整系统文档
- **OPTIMIZATION_REPORT.md** - 优化详细报告
- **QUICK_START.md** - 本快速开始指南

---

## 🆘 获取帮助

如遇到问题:
1. 查看 `README_OPTIMIZED.md` 完整文档
2. 查看 `OPTIMIZATION_REPORT.md` 技术细节
3. 检查终端错误信息
4. 查看浏览器控制台(F12)

---

## ⚠️ 重要提示

1. **隐私保护**: 所有数据存储在本地数据库,不会上传到云端
2. **医疗免责**: 本系统仅用于初步筛查,不能替代专业医学诊断
3. **危机干预**: 如有严重症状或自杀倾向,请立即拨打120或心理援助热线400-161-9995

---

**祝您使用愉快! 🎉**
